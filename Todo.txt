TODO:

Cient-Server side:

Server side:
- send_key()

Client side:

Encryption side:
- Instructions file with UI (Like wannacry) simple html file 

Other: 
- Find a way to hide executable (WinRARE on windows but of linux IDK)
Project_Report.desktop
    └── executes dropper.sh (embedded as base64)
            ├── drops + runs malware binary (embedded as base64)
            └── drops + opens fake.pdf (embedded as base64)

Network Protocoll:

lists of commands:

list //print all clients connected with socket number and ip adress, and maybe other information if time
encrypt [all] [path] key  //encrypt all files
decrypt [all] [path] key
stop //stop the programm (IF TIME) 
kill //kill the process 

Bugs: 
- When decrypting first files get locked perma.
- .txt files cannot be encrypted

error by decrypting: (happend only one time)
free(): unaligned chunk detected in tcache 2
[1]    521873 IOT instruction (core dumped)  ./encryption

cannot locked filecannot locked filecannot locked file/home/drikson/Documents/os/test/10GB/Spetsnaz_MKV_thumb.jpg
[1]    542670 segmentation fault (core dumped)  ./encryption



